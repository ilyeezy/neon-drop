// Единственное место с числами баланса (ТЗ п. 3 «Очки», п. 4.3): ядро и
// генератор читают отсюда, хардкода по модулям нет.
// Множители хранятся целыми ×100: арифметика очков ведётся в целых числах,
// цепочки double-умножений на 1.2/2.2 исключены классом (CORE-SCORE-004).
// @spec CORE-SCORE-005
export const BALANCE = {
  pointsPerCell: 1,
  clearBase: 10,
  // множитель за n линий одним ходом (×100): индекс n-1, последний — для 5+
  multiBonusX100: [100, 150, 220, 300, 400],
  // лестница стрика (×100): индекс — ступень
  streakLadderX100: [100, 120, 150, 200, 250, 300],
  emptyBoardBonusPerSize: 50,
  undoDepth: 5,
  // пороги заполненности для корзин весов генератора (ТЗ п. 4.3)
  generatorFill: { low: 0.25, high: 0.55 },
  generator: {
    maxAttempts: 20,        // общий лимит перегенераций (ТЗ 4.1, 4.4)
    lineBoost: 2.2,         // буст линий в тесной корзине (ТЗ 4.3)
    // Раздача сознательно щадящая: крупные фигуры давятся тем сильнее, чем
    // теснее поле, а мелочь всегда остаётся в достатке.
    tightPenalty: 1.0,      // степень подавления крупных при высокой заполненности
    smallBoost: 1.6,        // поддержка фигур в 1–2 клетки в тесноте
    easyCandidates: 4,      // сколько разыгрываемых троек сравнить и выбрать самую удобную
    largeSize: 5,           // «крупная» для анти-паттерна (ТЗ 4.4)
    solverNodeBudget: 20000, // лимит узлов DFS; прикидка, фиксируется по замеру автотеста
  },
};
